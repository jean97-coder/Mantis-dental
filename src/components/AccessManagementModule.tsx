import { useEffect, useState } from 'react';
import { Save, ShieldCheck } from 'lucide-react';
import type { ModuleId } from '../types/navigation';

interface UserAccess { id: number; email: string; name: string; role: 'admin' | 'odontologo'; permissions: string[] }
const API = 'http://localhost:4001/api/auth';
const modules: Array<{ id: ModuleId; label: string }> = [
  { id: 'pacientes', label: 'Pacientes' }, { id: 'agenda', label: 'Agenda' }, { id: 'presupuestos', label: 'Presupuestos' }, { id: 'colegas', label: 'Colegas' }, { id: 'finanzas', label: 'Finanzas' }, { id: 'whatsapp', label: 'WhatsApp' }, { id: 'reportes', label: 'Reportes' }, { id: 'inventario', label: 'Inventario' }, { id: 'configuracion', label: 'Configuración' },
];

export default function AccessManagementModule({ token }: { token: string }) {
  const [users, setUsers] = useState<UserAccess[]>([]);
  const [error, setError] = useState('');
  const [message, setMessage] = useState('');
  const [newUser, setNewUser] = useState({ name: '', email: '', password: '', role: 'odontologo' });
  useEffect(() => { fetch(`${API}/users`, { headers: { Authorization: `Bearer ${token}` } }).then(async (response) => { if (!response.ok) throw new Error('No se pudieron cargar los usuarios'); setUsers(await response.json() as UserAccess[]); }).catch((loadError) => setError(loadError instanceof Error ? loadError.message : 'Error al cargar usuarios')); }, [token]);
  function toggle(userId: number, moduleId: string) { setUsers((current) => current.map((user) => user.id === userId ? { ...user, permissions: user.permissions.includes(moduleId) ? user.permissions.filter((item) => item !== moduleId) : [...user.permissions, moduleId] } : user)); }
  async function save(user: UserAccess) { try { const response = await fetch(`${API}/users/${user.id}/permissions`, { method: 'PUT', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify({ permissions: user.permissions }) }); if (!response.ok) throw new Error('No se pudieron guardar los permisos'); setMessage(`Permisos de ${user.name} actualizados`); } catch (saveError) { setError(saveError instanceof Error ? saveError.message : 'Error al guardar permisos'); } }
  async function create() { try { const response = await fetch(`${API}/users`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify(newUser) }); const data = await response.json() as UserAccess | { error?: string }; if (!response.ok) throw new Error('error' in data && data.error ? data.error : 'No se pudo crear el usuario'); setUsers((current) => [...current, data as UserAccess]); setNewUser({ name: '', email: '', password: '', role: 'odontologo' }); setMessage('Usuario creado correctamente'); } catch (createError) { setError(createError instanceof Error ? createError.message : 'Error al crear usuario'); } }
  return <section className="mx-auto w-full max-w-7xl space-y-6"><header><div className="flex items-center gap-2 text-teal-600"><ShieldCheck className="h-5 w-5" /><span className="text-xs font-bold uppercase tracking-widest">Administración</span></div><h2 className="mt-1 text-2xl font-black text-slate-900">Gestión de Accesos</h2><p className="mt-1 text-sm text-slate-500">Crea usuarios y controla los módulos disponibles.</p></header>{error && <p className="rounded-xl bg-rose-50 px-4 py-3 text-sm text-rose-700">{error}</p>}{message && <p className="rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{message}</p>}<form onSubmit={(event) => { event.preventDefault(); void create(); }} className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:grid-cols-2 lg:grid-cols-5"><input required placeholder="Nombre" value={newUser.name} onChange={(event) => setNewUser((current) => ({ ...current, name: event.target.value }))} className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm" /><input required type="email" placeholder="Correo" value={newUser.email} onChange={(event) => setNewUser((current) => ({ ...current, email: event.target.value }))} className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm" /><input required minLength={8} type="password" placeholder="Contraseña" value={newUser.password} onChange={(event) => setNewUser((current) => ({ ...current, password: event.target.value }))} className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm" /><select value={newUser.role} onChange={(event) => setNewUser((current) => ({ ...current, role: event.target.value }))} className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><option value="odontologo">Odontólogo</option><option value="admin">Administrador</option></select><button type="submit" className="rounded-xl bg-teal-600 px-3 py-2.5 text-sm font-bold text-white">Crear usuario</button></form><div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Usuario</th><th className="px-5 py-4">Rol</th>{modules.map((module) => <th key={module.id} className="px-3 py-4 text-center">{module.label}</th>)}<th className="px-5 py-4">Acción</th></tr></thead><tbody className="divide-y divide-slate-100">{users.map((user) => <tr key={user.id}><td className="px-5 py-4"><strong className="block text-slate-900">{user.name}</strong><span className="text-xs text-slate-500">{user.email}</span></td><td className="px-5 py-4"><span className="rounded-full bg-teal-50 px-2 py-1 text-xs font-bold text-teal-700">{user.role}</span></td>{modules.map((module) => <td key={module.id} className="px-3 py-4 text-center"><input type="checkbox" disabled={user.role === 'admin'} checked={user.role === 'admin' || user.permissions.includes(module.id)} onChange={() => toggle(user.id, module.id)} className="h-4 w-4 accent-teal-600" /></td>)}<td className="px-5 py-4"><button type="button" disabled={user.role === 'admin'} onClick={() => void save(user)} className="inline-flex items-center gap-1 rounded-lg bg-teal-600 px-3 py-2 text-xs font-bold text-white disabled:opacity-40"><Save className="h-3.5 w-3.5" />Guardar</button></td></tr>)}</tbody></table></div></section>;
}